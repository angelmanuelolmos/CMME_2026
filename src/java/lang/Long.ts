

export class Long
{
    //public static MAX_LONG:number = Number.MAX_SAFE_INTEGER;  //not quite correct.  this is just the int portion of the float.

    public static MAX_SAFE_INTEGER:number = Number.MAX_SAFE_INTEGER;  //not quite correct.  this is just the int portion of the float.
}